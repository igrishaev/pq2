name: Build on macOS
on: [push]

jobs:
  build:
    runs-on: macos-latest # Specifies a GitHub-hosted macOS runner

    steps:
    - name: Check out repository code
      uses: actions/checkout@v4 # Use actions/checkout@v4 (or a later version)

    - name: Update Homebrew and install a formula (e.g., GitHub CLI)
      run: |
        brew update
        brew install gh # Installs the GitHub CLI
      # Homebrew is located in the PATH, so 'brew' commands work directly

    - name: Install a cask (e.g., Microsoft Edge)
      run: |
        brew install --cask microsoft-edge # Installs the Microsoft Edge application
